{
  "name": "boda-carolina-esthefano",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -r index.html styles.css assets dist/",
    "start": "npx serve .",
    "dev": "npx serve ."
  },
  "devDependencies": {}
}
