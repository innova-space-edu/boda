{
  "name": "boda-carolina-esthefano",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf out && mkdir -p out && cp -r index.html styles.css assets out/",
    "start": "npx serve .",
    "dev": "npx serve ."
  },
  "devDependencies": {}
}
